{
  "name": "wanderworld",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "nipplejs": "^0.10.2",
    "simplex-noise": "^4.0.3",
    "three": "^0.163.0"
  },
  "devDependencies": {
    "@types/three": "^0.163.0",
    "stats.js": "^0.17.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
